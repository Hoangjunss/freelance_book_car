import { CreateProductGroupRequest } from './create-product-group-request';

describe('CreateProductGroupRequest', () => {
  it('should create an instance', () => {
    expect(new CreateProductGroupRequest()).toBeTruthy();
  });
});
